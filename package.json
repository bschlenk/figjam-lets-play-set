{
  "name": "lets-play-set",
  "version": "1.0.0",
  "description": "Play the game of Set in FigJam",
  "author": "Brian Schlenker <bschlenk@umich.edu>",
  "license": "MIT",
  "main": "bundle.js",
  "scripts": {
    "build": "tsc && esbuild --bundle --minify dist/index.js > bundle.js",
    "start": "nodemon --watch src --ext 'ts,tsx' --exec 'npm run build'"
  },
  "devDependencies": {
    "@figma/plugin-typings": "file:plugin-typings",
    "esbuild": "^0.13.5",
    "nodemon": "^2.0.13",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "xstate": "^4.25.0"
  }
}
