{
  "name": "lets-play-set",
  "version": "1.0.0",
  "description": "Play the game of Set in FigJam",
  "author": "Brian Schlenker <bschlenk@umich.edu>",
  "license": "MIT",
  "main": "bundle.js",
  "scripts": {
    "build": "npm run check && esbuild --bundle --minify --target=es2016 src/index.ts > bundle.js",
    "check": "tsc",
    "start": "nodemon --watch src --ext 'ts,tsx' --exec 'npm run build'"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.42.1",
    "@figma/widget-typings": "^1.0.3",
    "esbuild": "^0.14.22",
    "nodemon": "^2.0.15",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "xstate": "^4.30.1"
  }
}
