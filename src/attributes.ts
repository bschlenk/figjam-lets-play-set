export const enum AttrCount {
  ONE,
  TWO,
  THREE,
}

export const enum AttrColor {
  RED,
  GREEN,
  PURPLE,
}

export const enum AttrShape {
  DIAMOND,
  OVAL,
  SQUIGGLE,
}

export const enum AttrShade {
  NONE,
  STRIPED,
  FILLED,
}
